# Hangman-game: Local (WSL) vs GitHub Discrepancy Report

**Date:** Investigation only — no file changes, no destructive commands.  
**Scope:** Local WSL `hangman-game` vs GitHub; excludes the separate `hangman-offline` variant you updated today.

---

## 1. Repository Status Summary

| Item | Result |
|------|--------|
| **Is Git repo** | Yes (`git rev-parse --is-inside-work-tree` → true) |
| **Current branch** | `cursor/progress-accuracy-mastery-selector` |
| **Remotes** | `origin` → `git@github.com:Phoenix-L/hangman-game.git` (fetch + push) |
| **Tracking** | Local branch tracks `origin/cursor/progress-accuracy-mastery-selector` |
| **Ahead/behind (tracked branch)** | **0 ahead, 0 behind** — same commit as remote |
| **Ahead/behind (vs origin/master)** | **2 commits ahead**, 0 behind |
| **Uncommitted changes** | **Yes** — 13 files modified or deleted (not staged) |
| **Untracked files** | **Yes** — 24 items (new data files, docs, offline assets, script, vocab.js) |

**Note:** `git fetch origin` was attempted but failed in this environment (SSH / host key). All remote refs used below are from a previous fetch. Re-run `git fetch origin` locally to refresh.

---

## 2. Local vs Remote Difference Summary

### 2.1 Committed state (already pushed)

- **HEAD** = `origin/cursor/progress-accuracy-mastery-selector` = `405388d`  
  - "Progress dashboard: per-theme accuracy, mastery by wrong_guesses<3, skip mastered-today in selector"
- **origin/master** = `7a1ea53`  
  - "Add progress summary API and shareable progress dashboard (#9)"

So on GitHub:

- Branch **master**: at `7a1ea53` (older).
- Branch **cursor/progress-accuracy-mastery-selector**: at `405388d` (your progress/accuracy work, already pushed).

Your local branch matches GitHub for `cursor/progress-accuracy-mastery-selector`; there are **no committed-but-unpushed** changes.

### 2.2 Uncommitted changes (working tree vs HEAD)

All differences between your working copy and GitHub are **uncommitted** (and unstaged).

| Type | Count | Files |
|------|--------|-------|
| **Modified (M)** | 6 | README.md, data/ket_animals.txt, data/ket_family.txt, data/ket_food.txt, data/ket_school.txt, game.js, style.css |
| **Deleted (D)** | 7 | data/pet_environment.txt, data/pet_hobbies.txt, data/pet_travel.txt, data/words/ket.txt, data/words/pet.txt, word/words.txt |

**Rough scope:** +346 lines, -119 lines across those 13 files.

### 2.3 Untracked files

| Category | Files / paths |
|----------|----------------|
| **New data (20 themes)** | data/ket_body.txt, ket_clothes.txt, ket_colors.txt, ket_environment.txt, ket_general.txt, ket_health.txt, ket_hobbies.txt, ket_home.txt, ket_jobs.txt, ket_nature.txt, ket_numbers.txt, ket_sports.txt, ket_technology.txt, ket_time.txt, ket_transport.txt, ket_travel.txt |
| **Offline / build** | index-offline.html, scripts/build_vocab_js.py, vocab.js |
| **Docs** | docs/ (contains docs/OFFLINE_MIGRATION_PLAN.md) |

---

## 3. Branch / Tracking Analysis

| Branch | Local commit | Remote | Relationship |
|--------|--------------|--------|--------------|
| **cursor/progress-accuracy-mastery-selector** (current) | 405388d | origin/cursor/progress-accuracy-mastery-selector @ 405388d | In sync |
| **master** | 7a1ea53 | origin/master @ 7a1ea53 | In sync |
| **cursor/auth-progress-ux-fixes** | 21dd783 | origin/cursor/auth-progress-ux-fixes | Exists both sides |

- **Default branch on GitHub:** `origin/HEAD` → `origin/master`.
- **Branch mismatch:** You are not on `master`; you are on `cursor/progress-accuracy-mastery-selector`. That is a feature branch, not a mismatch — it is tracked and pushed.
- **Divergence:** No divergence between local and remote for the current branch. All “difference” is in the **working tree** (uncommitted + untracked).

---

## 4. Offline Variant Related Files

These are the changes that belong to the **offline variant** (or its build/docs), not core Flask/backend behavior. Kept here only for labeling; your separate `hangman-offline` is out of scope.

**Fully offline / build / docs (untracked):**

- `index-offline.html`
- `vocab.js` (generated by build script)
- `scripts/build_vocab_js.py`
- `docs/` (e.g. `docs/OFFLINE_MIGRATION_PLAN.md`)

**Mixed (main repo file, but uncommitted edits add offline behavior):**

- **game.js** — uncommitted diff adds: `OFFLINE_MODE`, `themeDisplayName`, `selectWordOffline`, `computeLocalScore`, offline branch in `loadWord`, offline branch in `submitGameResult`, offline in `checkGameStatus`, `refreshAuth`, `loadProgressSummary`, `startGame`.
- **style.css** — uncommitted diff adds: `.auth-bar.offline-mode` rules.
- **README.md** — uncommitted diff adds: “Offline mode” section and “copy these files” list.

So:

- **Offline-variant–only (for separation):**  
  index-offline.html, vocab.js, scripts/build_vocab_js.py, docs/
- **Main hangman-game repo, but with uncommitted “offline” edits:**  
  game.js, style.css, README.md  
  (plus data/ and deletions below.)

**Data / vocabulary (main repo content, not “offline” packaging):**

- Uncommitted: changes and deletions in `data/` and `word/` (vocabulary expansion, PET removal, KET expansion, legacy clean-up).
- Untracked: new `data/ket_*.txt` files (20 themes).  
These are **main hangman-game repo** vocabulary changes, not the separate hangman-offline variant.

---

## 5. Risk Assessment

| Risk | Level | Notes |
|------|--------|------|
| **Losing uncommitted work** | High | All current diff is uncommitted; one `git checkout -- .` or `git reset --hard` would wipe it. |
| **Overwriting remote** | Low | You are not ahead of the remote; no force-push of current branch. |
| **Merge conflicts** | Low | No pull/merge was run; if you later merge `origin/master` into this branch, conflicts are possible in files touched in both (e.g. game.js, README). |
| **Wrong branch** | Low | You are intentionally on a feature branch; no evidence of confusion with master. |
| **Generated file in repo** | Low | `vocab.js` is generated; if committed, it can be regenerated from `data/*.txt` and `scripts/build_vocab_js.py`. |

---

## 6. Recommended Safe Fix Plan

Objectives:

1. Preserve all current work (data, code, offline).
2. Keep “main hangman-game” and “offline variant” separable where useful.
3. Leave the repo in a clear, non-destructive state.

### 6.1 Back up first

- Copy the whole repo or at least the working tree to a safe location (e.g. `hangman-game-backup-YYYYMMDD`), or create a patch:
  - `git diff > ~/hangman-game-uncommitted.patch`
  - `git status -uall > ~/hangman-game-untracked-list.txt`
- Optional: add a branch that records current state without overwriting:
  - `git stash push -u -m "WIP: data + offline + README"`  
  then later you can inspect with `git stash list` and `git stash show -p`.

### 6.2 What to commit (main hangman-game repo)

- **Data and vocabulary (main repo):**
  - Add the new `data/ket_*.txt` files.
  - Add the deletions (pet_*, data/words/*, word/words.txt) and modifications to existing data files.
  - Commit as one logical change, e.g. “Expand vocabulary to 20 KET themes, remove PET and legacy word lists.”
- **Offline support in main repo (optional but useful):**
  - If you want offline mode to live in this repo: commit `index-offline.html`, `scripts/build_vocab_js.py`, `docs/OFFLINE_MIGRATION_PLAN.md`, and the offline-related edits in `game.js`, `style.css`, and `README.md`.
  - Decide whether to commit `vocab.js` (regenerable) or add `vocab.js` to `.gitignore` and document “run build_vocab_js.py” in README.

### 6.3 What to move or copy into hangman-offline (if you use a separate folder)

- Copy (do not move, so hangman-game keeps a copy):  
  `index-offline.html`, `vocab.js`, `game.js`, `style.css`, and `assets/` (and any other assets the offline page needs).
- Optionally copy: `scripts/build_vocab_js.py`, `docs/OFFLINE_MIGRATION_PLAN.md`, and the “Offline mode” part of README, so hangman-offline is self-contained.

### 6.4 What to pull / merge

- After backing up and committing (or stashing):
  - `git fetch origin`
  - If you want this branch to include latest master:  
    `git merge origin/master` (or `git rebase origin/master` if you prefer; rebase is riskier if you are new to it).
- Resolve any conflicts in a separate commit; do not overwrite your data or offline changes.

### 6.5 What to ignore via .gitignore

- Consider adding **vocab.js** if you always regenerate it (so the repo stays clean and vocabulary is source-of-truth in `data/*.txt`).
- Do **not** add `data/` or `docs/` to .gitignore for this repo if you want them versioned.

### 6.6 What to leave untouched for now

- Do **not** run `git reset --hard`, `git clean -fd`, or any command that discards uncommitted or untracked files until you have backed up or committed.
- Do **not** delete the new `data/ket_*.txt` files or the offline-related files unless you have copied them elsewhere.

---

## 7. Exact Commands You Can Run Next (Safe Only)

No destructive or auto-commit steps are included. Run from repo root: `/home/ht/projects/hangman-game`.

**Inspect and back up (no changes to repo state):**

```bash
cd /home/ht/projects/hangman-game
git status
git status -uall > /tmp/hangman-untracked-list.txt
git diff --stat
git diff > /tmp/hangman-uncommitted.patch
```

**Optional: stash everything (uncommitted + untracked) to preserve state:**

```bash
git stash push -u -m "WIP: data expansion + offline mode + README"
git stash list
```

**Refresh remote refs (requires network and SSH):**

```bash
git fetch origin
git status -sb
git log --oneline -3 origin/cursor/progress-accuracy-mastery-selector
git log --oneline -3 origin/master
```

**Compare this branch to GitHub default branch:**

```bash
git log origin/master..HEAD --oneline
git diff origin/master...HEAD --stat
```

**List untracked files:**

```bash
git status -uall
```

No `git commit`, `git push`, `git reset --hard`, or `git clean` has been run or recommended in this section.

---

**End of report.** Use this to decide what to commit, what to copy to hangman-offline, and what to back up before any merge or cleanup.
